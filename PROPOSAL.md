# 多来源 NTP 时钟健康观测

本地申报候选材料，2026-09-22；模块 `xuting22/ntp`，版本 `0.4.0`。团队的公开仓库可能还是先前提交，本次没有推送；最终表单必须指向团队实际上传版本。

## 要解决的任务

应用节点需要知道与时间源的偏差、往返延迟及不确定度，而无需本工具直接修改系统时钟。多源过滤可以暴露单个异常时间源。

以下是目标任务和可复现工程证据，不虚构客户、存量部署或采用人数。

## 现有工作与新增贡献

本轮未找到同范围 MoonBit NTP/SNTP 包。贡献是观测接口和组合采样工作流，不是发明 NTP 或替代系统校时守护程序。 检索原始响应在总交付包的创新性复核目录保存。

MoonBit 负责报文/时间戳、八级过滤和多来源选择；Node 负责 UDP、认证 MAC 和采样调度。



## 可复现路径

仓库附编译引擎；修改源码后先构建。参考工具的额外依赖与环境变量见 TESTING.md；测试创建的网络服务仅在本机。

```sh
node tools/test-sync.mjs
```

本轮 8 组真实本机 UDP/MAC、多源异常钟剔除、KoD 和 CLI 检查通过，不连接公共 NTP 服务。 本轮 JS/WasmGC 核心测试及 JS 构建通过，原始日志见 [本轮验证](evidence/innovation-review-20260922/results.json)。测试数量证明所列范围，不能代替创新性论证或推断正式审核通过。

## 边界与来源

没有 NTS，不等同 chronyd；估计值会受网络不对称等影响。认证 MAC 的覆盖与系统时钟精度分开。

许可证与来源沿用仓库现有 LICENSE/第三方说明，不将标准、算法、词库或参考软件写成本项目发明。查重不是对全生态不存在的证明，日期、相邻项与未覆盖范围见 [DUPLICATION.md](DUPLICATION.md)。

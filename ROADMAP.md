# 持续追平清单

0.4.0 补上了扩展字段、共享密钥认证、八级过滤、多来源选择、批量采样、工作台及独立服务互通。完整成熟度目标仍未完成。

1. NTS：NTS-KE、TLS 身份验证、cookie 生命周期与 AEAD 扩展，使用独立实现验证。
2. 完整 NTP peer/system 状态机：popcorn rejection、主来源滞回、自适应轮询、PLL/FLL、闰秒策略；系统时钟修改需要另行确定运行环境与授权。
3. 更多平台与故障环境：跨主机延迟/丢包/重排、长期运行、DNS 变化、实际 IPv6 时间服务与多个独立 daemon。
4. 精度与性能：硬件/内核时间戳、明确工作负载下的长期误差和吞吐比较。当前微基准只说明本机执行时间。
5. 独立打包与发布流程：现有 CI 配置尚未在远端运行；不从本地检查推断公开包或比赛验收。当前只做本地开发，不上传、不发布。

旧 ZIP/bundle 是历史快照，未随本轮重新打包。其他 19 个仓库本轮没有重复验收。
